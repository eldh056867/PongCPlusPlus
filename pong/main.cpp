#include <iostream> // Includes the standard input/output stream library for console operations
#include "Game.h"   // Includes the custom Game class header

using namespace std; // Use the standard namespace for easier access to standard library components

// Entry point of the application
int main()
{
    Game game;  // Create an instance of the Game class
    game.main(); // Call the main function of the Game class to start the game

    return 0; // Return 0 to indicate successful execution
}
